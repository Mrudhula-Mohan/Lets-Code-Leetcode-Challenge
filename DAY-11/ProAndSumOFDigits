class Solution(object):
    def subtractProductAndSum(self, n):
        """
        :type n: int
        :rtype: int
        """
        s=0
        pro=1
        while n > 0:
            digit = n % 10
            s += digit
            pro *= digit
            n = n // 10
            
        return pro - s
