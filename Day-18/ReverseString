class Solution(object):
    def reverseWords(self, s):
        """
        :type s: str
        :rtype: str
        """
        words = s.split()
        rev_words = []

        for w in words:
           rev_words.append(w[::-1])

        result = " ".join(rev_words)
        return result
